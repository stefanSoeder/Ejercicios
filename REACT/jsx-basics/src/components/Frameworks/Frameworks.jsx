const arrayFrameworks = [`Gatsby, Next, Angular`]

const Frameworks = (arrayFrameworks) => {
    arrayFrameworks.forEach((framework) return (
    <div>
        <h2>{framework}</h2>
    </div>)
    )}
